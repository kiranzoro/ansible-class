- name: Install Nginx
  hosts: localhost
  become: yes
  tasks:
  - name: install nginx latest version
    ansible.builtin.dnf:
      name: 
      - nginx
      - mysql
      - unzip
      state: latest
  - name: start nginx service
    ansible.builtin.service:
      name: nginx
      state: started    